import { EnumToSelectorPipe } from './enum-to-selector.pipe';

describe('EnumToSelectorPipe', () => {
  it('create an instance', () => {
    const pipe = new EnumToSelectorPipe();
    expect(pipe).toBeTruthy();
  });
});
